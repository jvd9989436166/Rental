{
  "name": "rentalmate",
  "version": "1.0.0",
  "description": "Modern PG & Room Booking Platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "json-server": "json-server --watch db.json --port 3001",
    "dev": "live-server --port=3000"
  },
  "keywords": ["pg", "booking", "rental"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "json-server": "^0.17.4"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
